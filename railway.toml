[build]
builder = "NIXPACKS"
buildCommand = """
apt-get update && \
apt-get install -y tesseract-ocr libgl1 && \
pip install -r requirements.txt
"""

[env]
PYTHONUNBUFFERED = "1"

[service]
startCommand = "gunicorn split:app"
